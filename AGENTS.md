# 翔仔居家公司工具：後續開發守則

本檔是給未來接手這個儲存庫的 Codex、AI 開發工具與工程師看的。開始修改前，必須先讀完本檔與 `README.md`，再檢查實際 Git branch、工作區變更及目前部署狀態；不要只依賴舊對話記憶。

## 專案目的與現況

- `https://siangstock.com/` 是可持續增加工具的公司入口。
- `/inventory/` 是「歷史庫存資料清洗」，Excel 全程只在使用者瀏覽器處理。
- `/inventory/rules-admin/` 是受 Cloudflare Access 保護的集中規則管理頁。
- `siangstock.com/api/rules*` 才由 Cloudflare Worker 處理；其餘網站由 Zeabur 提供靜態檔。
- 現行 GitHub 部署分支是 `feature/company-tools-static-site`。每次作業仍需重新查證，不可假設分支永遠不變。
- `/orders/`、`/reports/` 是未來工具的預留方向，不代表已授權建立或部署。

## 不可破壞的安全界線

1. 不得把真實 Excel、檔名、庫存列資料、輸出檔、客戶資料、密碼、Token、API key 或私密環境設定提交到公開 repo。
2. 庫存 Excel 必須留在瀏覽器本機；不得新增 Excel／檔案上傳 API、後端儲存或 request body log。
3. 不得改寫使用者選取的原始 Excel；結果一律下載成新檔。
4. 不得因新增其他工具而放寬既有 CSP、Cloudflare Access、Worker JWT 驗證或 API fail-closed 行為。
5. 未經當次明確授權，不得修改 DNS、Cloudflare route、Access、D1、Zeabur、正式網域、GitHub 公開狀態或舊服務。
6. 不得刪除舊服務、資料庫、Volume、repo、branch、commit 或歷史資料；需要替換時先記錄可回復基準。

## 新增工具的固定架構

新增工具時，優先使用獨立的小寫英文路徑，例如：

```text
/
├── inventory/        歷史庫存資料清洗
├── orders/           訂單工具（未來）
└── reports/          報表工具（未來）
```

每個新工具至少要完成：

- 在根目錄首頁新增一張清楚的工具入口卡片。
- 建立自己的路徑與頁面，不破壞其他工具的 HTML、CSS、JavaScript。
- 加入「公司工具首頁 → 目前工具 → 子功能」麵包屑與清楚返回按鈕。
- 介面與操作說明使用繁體中文、白話，適合非工程師。
- 說明資料在哪裡處理、是否登入、是否會傳到伺服器。
- 為核心規則、輸入輸出及返回導覽增加自動化測試。
- 先在本機／測試網址驗證，再取得明確授權後提交、推送及部署。

如果工具需要後端、登入、共用資料或外部 API，必須先提出最小安全架構、資料流、權限、成本與回復方式，取得使用者同意後才能新增雲端資源。

## 標準工作流程

1. 盤點：確認 repo、branch、乾淨／既有變更、相關路徑與部署方式。
2. 規劃：列出使用情境、輸入、輸出、權限、資料是否離開裝置及失敗行為。
3. 實作：只修改本次需求相關檔案，保留既有工具。
4. 測試：執行相關自動化測試並實際走過主要操作流程。
5. 發布前核對：檢查 diff，排除真實資料、測試輸出與敏感設定；只 stage 精確檔案。
6. 發布：提交、推送、部署各自都需有明確授權；不得把其中一項授權自行擴大成全部授權。
7. 上線驗證：驗證正式網址、HTTPS、主要流程、安全界線與回復方式。

既有 Cloudflare 測試指令：

```bash
cd cloudflare
pnpm check
pnpm test
```

若新增工具沒有使用 Cloudflare，也要為該工具提供對等的可重複測試方式。

## 未來需求的辨識關鍵字

使用者說出「新增公司工具」時，視為啟動規劃，不代表自動取得推送、部署或修改雲端資源的授權。接手者應從需求或現況取得以下資訊；只有會實質影響結果、且無法安全推定的項目才需要追問：

- 工具名稱與使用者。
- 想解決的工作。
- 輸入資料與期望輸出。
- 資料可否上雲；若未說明，敏感公司資料一律預設不上傳。
- 是否需要登入、共用資料或管理權限。
- 本次只規劃、做測試版，或已授權發布正式站。

最短但足夠的需求範例：

```text
新增公司工具：訂單整理。給客服同事使用，輸入 xlsx，輸出整理後的新 xlsx；資料不能上傳。先做測試版，不要部署。
```

## 完成定義

完成回報至少包含：變更內容、網址或成品位置、測試結果、資料安全確認、commit（若有發布）、已知限制，以及能實際執行的回復方式。未通過測試、仍需使用者決定或缺少必要權限時，必須明確標示尚未完成。
